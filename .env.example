# Optional. Required only if you enable the SAM.gov collector.
SAM_API_KEY=

# Optional. Required only if you enable the LLM executive summary (llm.enabled).
ANTHROPIC_API_KEY=

# Optional email delivery.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=
